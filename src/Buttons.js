import React from "react";

function Buttons () {
    return (
      <button
      type="button"
      onClick={() => console.log("to the collection")}> to the collection
      </button>
    )
}

export default Buttons;
